<?xml version="1.0" encoding="utf-8"?>
<resources>

    <declare-styleable name="UIComponentInputField">
        <attr name="titleHint" format="string" />
        <attr name="inputHint" format="string" />
        <attr name="inputRightIcon" format="reference" />
    </declare-styleable>

    <declare-styleable name="UIComponentCloseBtn">
        <attr name="setImageResource" format="reference" />
    </declare-styleable>

    <declare-styleable name="UIComponentCircularGradientButton">
        <attr name="setImage" format="reference" />
    </declare-styleable>

    <declare-styleable name="UIComponentUsers" />

    <declare-styleable name="UIComponentAddPhoto" />

    <declare-styleable name="CardView">
        <!-- Shadow start color of CardView. -->
        <attr name="cardShadowStartColor" format="color" />
        <!-- Shadow end color of CardView. -->
        <attr name="cardShadowEndColor" format="color" />
        <!-- Background color for CardView. -->
        <attr name="cardBackgroundColor" format="color" />
        <!-- Corner radius for CardView. -->
        <attr name="cardCornerRadius" format="dimension" />
        <!-- Elevation for CardView. -->
        <attr name="cardElevation" format="dimension" />
        <!-- Maximum Elevation for CardView. -->
        <attr name="cardMaxElevation" format="dimension" />
        <!-- Add padding in API v21+ as well to have the same measurements with previous versions. -->
        <attr name="cardUseCompatPadding" format="boolean" />
        <attr name="showCardViewDefaultShadow" format="boolean" />
        <!-- Add padding to CardView on v20 and before to prevent intersections between the Card content and rounded corners. -->
        <attr name="cardPreventCornerOverlap" format="boolean" />
        <!-- Inner padding between the edges of the Card and children of the CardView. -->
        <attr name="contentPadding" format="dimension" />
        <!-- Inner padding between the left edge of the Card and children of the CardView. -->
        <attr name="contentPaddingLeft" format="dimension" />
        <!-- Inner padding between the right edge of the Card and children of the CardView. -->
        <attr name="contentPaddingRight" format="dimension" />
        <!-- Inner padding between the top edge of the Card and children of the CardView. -->
        <attr name="contentPaddingTop" format="dimension" />
        <!-- Inner padding between the bottom edge of the Card and children of the CardView. -->
        <attr name="contentPaddingBottom" format="dimension" />
        <!-- Workaround to read user defined minimum width -->
        <attr name="android:minWidth" />
        <!-- Workaround to read user defined minimum height -->
        <attr name="android:minHeight" />
    </declare-styleable>

</resources>